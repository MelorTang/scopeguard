{
  "name": "@scopeguard/server",
  "version": "0.4.0-preview",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter @scopeguard/shared build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @scopeguard/shared build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@scopeguard/core": "workspace:*",
    "@scopeguard/shared": "workspace:*"
  }
}
